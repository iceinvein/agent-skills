---
type: file_exists
# The design is written on the answer, so a spec on disk before it means the
# run drafted across the fork instead of asking it.
path: 'docs/specs/*.md'
exists: false
weight: 2
---
