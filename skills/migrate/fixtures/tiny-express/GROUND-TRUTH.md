# Ground truth for tiny-express

Two routes and one table. Any enumeration run over this fixture must find
exactly these, and the census must balance at these totals.

| surface | id | element |
| --- | --- | --- |
| routes | route-get-api-users | GET /api/users |
| routes | route-post-api-users | POST /api/users |
| tables | table-users | users |
